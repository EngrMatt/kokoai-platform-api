# =========================================================
# .env_example
# Please copy this file to .env and fill in your actual secrets.
# This file should NOT be committed to Git.
# ---------------------------------------------------------

# ---------------------------------------------------------
# PostgreSQL Service Configuration
# These variables are used for DB container environment settings (POSTGRES_*) 
# and for constructing the .NET API connection string.
# ---------------------------------------------------------

# POSTGRES_USER: The name for the database superuser.
POSTGRES_USER=your_username_here

# POSTGRES_PASSWORD: The database administrative password. Use a strong secret.
POSTGRES_PASSWORD=your_password_here # Recommended to be replaced with a more random secret in production.

# POSTGRES_DB: The default database name to be created.
POSTGRES_DB=your_db_name_here

# ---------------------------------------------------------
# Docker Compose Internal Connection Configuration (For API container access)
# ---------------------------------------------------------

# DB_HOST: The hostname the API container uses to connect to the DB container.
# In the default Docker Compose network, this value MUST be the name of the DB service (which is "db").
DB_HOST=db 

# ---------------------------------------------------------
# .NET Application Environment
# ---------------------------------------------------------

# ASPNETCORE_ENVIRONMENT: Determines the .NET application's environment (e.g., Development, Production).
ASPNETCORE_ENVIRONMENT=Development